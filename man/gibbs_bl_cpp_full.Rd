% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gibbs_bl_cpp_full.R
\name{gibbs_bl_cpp_full}
\alias{gibbs_bl_cpp_full}
\title{Full Bayesian Lasso with Gibbs Sampling via Rcpp}
\usage{
gibbs_bl_cpp_full(
  n_mcmc,
  start_lst,
  warm = 0,
  thin = 1,
  number_par = length(name_par),
  name_par,
  y,
  X,
  r = 0.5,
  delta = 0.5,
  a_sig = 0.5,
  b_sig = 0.5,
  metro = FALSE,
  metro.tune = NULL,
  n_chains = 1
)
}
\arguments{
\item{n_mcmc}{Number of MCMC iterations}

\item{start_lst}{List of starting values containing beta, tau2, lam2, and sig2}

\item{warm}{Number of warmup iterations to discard}

\item{thin}{Thinning interval for MCMC chain}

\item{number_par}{Total number of parameters}

\item{name_par}{Vector of parameter names}

\item{y}{Response vector}

\item{X}{Design matrix}

\item{r}{Parameter for Bayesian Lasso prior (default = 0.5)}

\item{delta}{Parameter for Bayesian Lasso prior (default = 0.5)}

\item{a_sig}{Shape parameter for inverse gamma prior on sig2 (default = 0.5)}

\item{b_sig}{Scale parameter for inverse gamma prior on sig2 (default = 0.5)}

\item{metro}{Logical indicating whether to use Metropolis step (default = FALSE)}

\item{metro.tune}{Tuning parameter for Metropolis step (default = NULL)}

\item{n_chains}{Number of MCMC chains to run (default = 1)}
}
\value{
List containing:
  \item{draws}{Matrix of posterior draws}
  \item{n_mcmc}{Number of MCMC iterations}
  \item{warm}{Number of warmup iterations}
  \item{thin}{Thinning interval}
  \item{sampleidx}{Indices of kept samples}
}
\description{
Implements Gibbs sampling for Bayesian Lasso regression using Rcpp
}
\examples{
# Generate example data
n <- 100
p <- 10
X <- matrix(rnorm(n*p), n, p)
beta_true <- c(rep(1, 3), rep(0, p-3))
y <- X \%*\% beta_true + rnorm(n, 0, 1)

# Set starting values
start <- list(
  beta = rep(0, p),
  tau2 = rep(1, p), 
  lam2 = 1,
  sig2 = 1
)

# Run MCMC
result <- gibbs_bl_cpp_full(
  n_mcmc = 1000,
  start_lst = start,
  warm = 100,
  thin = 2,
  name_par = paste0("beta", 1:p),
  y = y,
  X = X
)
}
