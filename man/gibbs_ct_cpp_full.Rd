% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gibbs_ct_cpp_full.R
\name{gibbs_ct_cpp_full}
\alias{gibbs_ct_cpp_full}
\title{Complex-valued Student-t Gibbs Sampler}
\usage{
gibbs_ct_cpp_full(
  n_mcmc,
  start_lst,
  warm = 0,
  thin = 1,
  number_par = length(name_par),
  name_par,
  y,
  X,
  rho_b = 0,
  rho_ep = 0,
  df = 1,
  a_sig = 0.5,
  b_sig = 0.5,
  learn_rho = FALSE,
  n_chains = 1,
  keep.re = 0,
  keep.im = 0,
  tune.re = 0.2,
  tune.im = 0.2,
  tune.len = 50,
  target.accept.rate = 0.3,
  adapt = TRUE,
  adapt_step = 0.2
)
}
\arguments{
\item{n_mcmc}{Integer. Number of MCMC iterations}

\item{start_lst}{List. Initial values containing:
- beta: Initial complex-valued regression coefficients
- tau2: Initial scale parameter
- sig2: Initial variance parameter}

\item{warm}{Integer. Number of warm-up draws to discard (default: 0)}

\item{thin}{Integer. Keep every thin-th draw for thinning (default: 1)}

\item{number_par}{Integer. Total number of parameters (default: length(name_par))}

\item{name_par}{Character vector. Names of parameters}

\item{y}{Complex vector/matrix. Response variable (n x 1)}

\item{X}{Numeric matrix. Design matrix (n x p)}

\item{rho_b}{Numeric. Prior correlation for beta (default: 0)}

\item{rho_ep}{Numeric. Prior correlation for error term (default: 0)}

\item{df}{Numeric. Degrees of freedom for Student-t distribution (default: 1)}

\item{a_sig, b_sig}{Numeric. Shape and scale parameters for sig2 prior (default: 0.5)}

\item{learn_rho}{Logical. Whether to learn correlation parameters (default: FALSE)}

\item{n_chains}{Integer. Number of MCMC chains (default: 1)}

\item{keep.re, keep.im}{Numeric. Initial acceptance rates for real and imaginary parts (default: 0)}

\item{tune.re, tune.im}{Numeric. Initial tuning parameters for MH steps (default: 0.2)}

\item{tune.len}{Integer. Frequency of adaptive tuning (default: 50)}

\item{target.accept.rate}{Numeric. Target acceptance rate for MH steps (default: 0.3)}

\item{adapt}{Logical. Whether to use adaptive tuning (default: TRUE)}

\item{adapt_step}{Numeric. Step size for adaptive tuning (default: 0.2)}
}
\value{
List containing:
\itemize{
  \item draws - Matrix of posterior draws
  \item n_mcmc - Number of MCMC iterations
  \item warm - Number of warm-up iterations
  \item thin - Thinning interval
  \item sampleidx - Vector indicating which iterations were kept
}
}
\description{
Implements Gibbs sampling for complex-valued data with Student-t
using Rcpp for computational efficiency.
}
